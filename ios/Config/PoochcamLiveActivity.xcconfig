#include "Base.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(BASE_PRODUCT_BUNDLE_IDENTIFIER).LiveActivity

// Retain the inherited Swift module while renaming Xcode labels.
PRODUCT_MODULE_NAME = MoblinLiveActivity
