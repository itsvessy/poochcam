#include "Base.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(BASE_PRODUCT_BUNDLE_IDENTIFIER).Moblin-Capture
CODE_SIGN_ENTITLEMENTS = Poochcam Screen Recording/Poochcam Screen Recording.$(CAPABILITIES).entitlements

PRODUCT_MODULE_NAME = Poochcam_Screen_Recording
